import { SheetHeader, SheetTitle, SheetDescription } from "@/components";

export function CreateProjectFormHeader() {
    return (
        <SheetHeader>
            <SheetTitle>Create Project</SheetTitle>
            <SheetDescription>
                Create the framework for a new project.
            </SheetDescription>
        </SheetHeader>
    );
}
